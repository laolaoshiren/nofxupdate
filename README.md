🚀 一键安装指南 (推荐)

在您的 Debian/Ubuntu 服务器上（确保已安装 git），以 root 权限 或 sudo 执行以下一行命令即可完成安装：

sudo bash -c "$(curl -fsSL https://raw.githubusercontent.com/laolaoshiren/nofxupdate/refs/heads/main/jb_install.sh)"
